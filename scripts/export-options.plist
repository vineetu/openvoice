<?xml version="1.0" encoding="UTF-8"?>
<!--
  Jot export options for `xcodebuild -exportArchive`.

  v1 ships ad-hoc signed, so we use method=mac-application (no Developer ID)
  with automatic signing style. This mirrors the Release config in the Xcode
  project (CODE_SIGN_IDENTITY = "-", ENABLE_HARDENED_RUNTIME = YES).

  When we add Developer ID (see docs/plans/apple-signing.md), switch to:
    <key>method</key>           <string>developer-id</string>
    <key>signingStyle</key>     <string>manual</string>
    <key>teamID</key>           <string>YOUR_TEAM_ID</string>
    <key>signingCertificate</key><string>Developer ID Application</string>
  and plan to notarize + staple after export.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>method</key>
    <string>mac-application</string>
    <key>signingStyle</key>
    <string>automatic</string>
    <key>destination</key>
    <string>export</string>
</dict>
</plist>
